import Element from '../Element/Element';
import './Node.css';

export default class Node extends Element {
    constructor(props) {
        super(props)
        // this.state = {
        //     ...this.state,

        // }
    }
}